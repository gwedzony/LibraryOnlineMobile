<?xml version="1.0" encoding="utf-8"?>

<toolkit:Popup   
                xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
                xmlns:local="clr-namespace:MauiApp1.Pages.Shared"
                x:Class="MauiApp1.Pages.Shared.PickABookcase">
    <StackLayout>
        <Label Text="Wybierz do ktorej biblioteki chcesz dodać" />
        <Picker ItemsSource="{Binding BookcasesDtos}" ItemDisplayBinding="{Binding Name}" SelectedItem="{Binding PickedBookcase}"></Picker>
        <Button Text="Dodaj" 
                Command="{Binding AddToBookcasesCommand}" 
                CommandParameter="{Binding Source={RelativeSource AncestorType={x:Type local:PickABookcase}},Path=BookId}"
                
                />

    </StackLayout>
</toolkit:Popup>